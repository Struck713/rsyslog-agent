plugins {
    id 'java'
}

group 'com.noah.syslog'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.dblock.log4jna:log4jna:2.1.0';
    implementation 'org.apache.log4jna:Win32EventLogAppender:2.1.0@dll';
    //implementation group: 'org.apache.log4jna', name: 'Win32EventLogAppender' version: '2.1.0', type: ''
}